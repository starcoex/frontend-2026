# 의존성 (빌드 시 npm ci로 재설치)
node_modules
**/node_modules

# 빌드 결과물 (도커 내부에서 새로 빌드)
dist
**/dist
build
**/build

# NX 캐시
.nx
**/.nx
.cache
**/.cache

# Git
.git
.gitignore

# 환경변수 파일 (보안)
.env
.env.*
!.env.example

# 로그
*.log
npm-debug.log*

# 에디터 설정
.vscode
.idea
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# 테스트 관련
coverage
**/__tests__
**/*.spec.ts
**/*.test.ts
**/e2e

# 도커 관련 (불필요한 재귀 복사 방지)
Dockerfile
.dockerignore
docker-compose*.yml

# K8s 매니페스트 (런타임 불필요)
k8s
